export const MCP = {
  INSTRUCTIONS: `
    This is a memo tools.
    You can create, read, update, and delete memos.
    If the user requests any operation related to memos (create, retrieve, update, delete, or search), you must use the tools provided by this MCP server.
    Do not manipulate memos directly via the database or by any other means.
    If the user asks you to record a memo, consider using this tool instead of writing directly to a file.
  `,
}
